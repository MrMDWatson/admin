import React from 'react'
import { connect } from 'react-redux'

export const Betbox = (props) => {
  return (
    <div>Betbox</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Betbox)